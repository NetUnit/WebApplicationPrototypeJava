# WebApplicationPrototypeJava


# POST
# Testing Blogs API: fetch('/message', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ text: 'Fourth message'})}).then(result => console.log(result));

# PUT
# Testing Blogs API: fetch('/message/4', { method: 'PUT', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ text: 'Some message to refresh the object', id: 10})}).then(result => console.log(result));

# DELETE
# Testing Blogs API: fetch('/message/6', { method: 'DELETE', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ text: 'Some message to refresh the object', id: 10})}).then(result => console.log(result));

